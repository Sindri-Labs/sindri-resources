The code in `src/main.rs` will create a circuit, upload, and initiate compile. To run, you can call
```
cargo run
```
which builds the executable (in debug mode) and runs.

TODO:
* Polling logic
* Initiating a Proof
* Verifying